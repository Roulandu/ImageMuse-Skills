# 破壁技能 V2 升级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实施 5 项改进：图片风格默认值（单反）、关键词命中跳过子路线、一键生成快速模式、反向破壁、AI 生成界面子路线

**Architecture:** 主力改动在 SKILL.md（4 项流程/创意规则），新建 1 个子路线文件，修改 3 个现有子路线追加反向模板，修改 1 个 registry。全是 markdown 编辑，无代码编译。

**Tech Stack:** Markdown 技能文件编辑，验证方式为 subagent 端到端测试

**Base path:** `.opencode/skills/image2-reality-breakthrough/`

## Global Constraints

- 所有改动需同步到 6 个副本目录（.agents/ .claude/ .codex/ .cursor/skills/ .cursor/ skills/）
- 每项改动完成后用 subagent 运行端到端验证
- 默认图片风格：专业单反相机写真拍摄质感（非 iPhone）
- 保持现有破壁第一法则、质量门、红线不变

---

### Task 1: #4 图片风格默认值改为单反

**Files:**
- Modify: `SKILL.md:165-166` (默认值与覆盖规则)
- Modify: `SKILL.md:225-230` (图片风格二选一)

**Interfaces:**
- Consumes: 无
- Produces: 图片风格默认值 `单反相机写真拍摄质感`，未指定时自动使用，输出附带提示

- [ ] **Step 1: 修改默认值与覆盖规则中的图片风格条目**

将第 165 行：
```
- **图片风格默认**：无默认值，必须由用户二选一
```
改为：
```
- **图片风格默认**：默认使用「专业单反相机的写真拍摄质感」。用户未指定时自动使用默认值，输出附带提示「默认使用单反写真质感，可随时改为 iPhone 直出质感」
```

- [ ] **Step 2: 修改图片风格二选一节**

将第 225-230 行：
```
## 图片风格二选一

用户未指定图片风格时，必须让用户从以下两个字面常量中选择一个：

1. `整张照片必须呈现着iPhone手机直出的拍摄质感...`
2. `整张照片必须呈现着专业单反相机的写真拍摄质感...`
```
改为：
```
## 图片风格

默认使用专业单反相机写真拍摄质感，输出附带提示「默认使用单反写真质感，可随时改为 iPhone 直出质感」。用户指定时完全覆盖。

可选风格常量：
1. `整张照片必须呈现着iPhone手机直出的拍摄质感，不要出现AI痕迹， 不要过度磨皮，不要美颜滤镜`
2. `整张照片必须呈现着专业单反相机的写真拍摄质感，不要出现AI痕迹， 不要过度磨皮，不要过度美颜滤镜`
```

- [ ] **Step 3: 修改工作流步骤 7**

将第 58 行：
```
7. 检查图片风格（iPhone 直出 / 单反质感）：未指定则让用户二选一
```
改为：
```
7. 锁定图片风格：用户指定 → 使用用户指定；未指定 → 默认单反写真质感，不阻塞流程
```

- [ ] **Step 4: 修改分步交互规则**

将第 65-69 行：
```
以下三种未完成状态，仅返回对应选择项，不生成最终提示词：

- **路线未选**...
- **子路线未选**...
- **图片风格未选**（路线已选、子路线已选、图片风格缺失）：返回图片风格二选一选项
```
改为：
```
以下两种未完成状态，仅返回对应选择项，不生成最终提示词：

- **路线未选**（大路线缺失）：返回 8 大路线的中文名 + 一句话简介，供用户选择
- **子路线未选**（大路线已选、子路线缺失）：返回该大路线下所有子路线的中文名 + 核心触发关键词，供用户选择

图片风格不再阻塞流程：未指定时默认使用单反写真质感。
```

- [ ] **Step 5: 验证**

用 subagent 发送请求 "生成一张从小红书里破壁出来的女孩"，检查是否不再问图片风格二选一，直接输出单反质感提示词。

- [ ] **Step 6: Commit**

```bash
git add .opencode/skills/image2-reality-breakthrough/SKILL.md
git commit -m "feat: 图片风格默认值改为单反，不再阻塞流程"
```

---

### Task 2: #5 关键词命中跳过子路线选择

**Files:**
- Modify: `SKILL.md:56` (工作流步骤 5)
- Modify: `SKILL.md:63-69` (分步交互规则)

**Interfaces:**
- Consumes: 步骤 4 锁定的路线 ID + 子路线关键词列表
- Produces: 子路线匹配逻辑增强（唯一匹配 → 直接锁定，多匹配/模糊 → 返回选项）

- [ ] **Step 1: 修改工作流步骤 5**

将第 56 行：
```
5. 识别子路线：匹配成功则进入步骤 6；匹配失败则返回该大路线下所有子路线选项
```
改为：
```
5. 识别子路线：用户输入关键词在当前大路线下唯一命中一个子路线 → 直接锁定，跳过子路线选择；匹配多个或无法确定 → 返回该大路线下所有子路线选项供用户选择
```

- [ ] **Step 2: 修改分步交互规则中的子路线条目**

将第 68 行：
```
- **子路线未选**（大路线已选、子路线缺失）：返回该大路线下所有子路线的中文名 + 核心触发关键词，供用户选择
```
改为：
```
- **子路线未选**（大路线已选、子路线缺失且用户输入无法唯一匹配）：返回该大路线下所有子路线的中文名 + 核心触发关键词，供用户选择。若用户输入关键词可唯一命中（如"手机"唯一命中 phone-screen），则直接锁定，不返回选择列表
```

- [ ] **Step 3: 验证**

用 subagent 发送请求 "从手机里破壁出来"，检查是否跳过子路线选择直接输出 phone-screen 提示词。再发送 "从屏幕里出来"，检查是否返回三选一。

- [ ] **Step 4: Commit**

```bash
git add .opencode/skills/image2-reality-breakthrough/SKILL.md
git commit -m "feat: 关键词唯一命中时跳过子路线选择"
```

---

### Task 3: #11 一键生成快速模式

**Files:**
- Modify: `SKILL.md:50-61` (工作流 — 插入快速模式检测)

**Interfaces:**
- Consumes: 步骤 2（解析用户输入结果）
- Produces: 快速模式检测逻辑，条件满足时跳过步骤 3-7 直接组装

- [ ] **Step 1: 在工作流中插入快速模式检测步骤**

将第 50-61 行：
```
## 工作流

1. 加载核心契约文件（按 Required load order 所列顺序）
2. 解析用户输入，锁定安全参数（根据 `parameter-schema.md`）
3. 识别大路线（8 选 1）：匹配成功则进入步骤 4；匹配失败则返回 8 个大路线选项供用户选择
4. 加载大路线 `./routes/{routeId}/_registry.md`
5. 识别子路线：...
6. 加载子路线文件...
7. 锁定图片风格...
8. 组装输出契约（按 `output-contract.md` 规定的格式）
9. 应用质量门（通用质量门 + 破壁专项质量门）
10. 返回结果
```
改为：
```
## 工作流

1. 加载核心契约文件（按 Required load order 所列顺序）
2. 解析用户输入，锁定安全参数（根据 `parameter-schema.md`）
3. **快速模式检测**：用户输入同时满足以下条件 → 跳过步骤 4-7，直接进入步骤 8（组装输出）：
   - 包含"破壁"关键词
   - 包含明确的路线/媒介关键词（可唯一匹配大路线+子路线）
   - 无额外定制需求关键词（如"有创意""柔美""科技感""震撼""换个姿势""斜侧"等）
   - 快速模式默认值：子路线默认过渡模板 + 半身用 lean-forward / 全身用 step-out + 单反质感 + 子路线默认画幅
4. 识别大路线（8 选 1）：匹配成功则进入步骤 5；匹配失败则返回 8 个大路线选项供用户选择
5. 加载大路线 `./routes/{routeId}/_registry.md`
6. 识别子路线：用户输入关键词在当前大路线下唯一命中一个子路线 → 直接锁定；匹配多个或无法确定 → 返回该大路线下所有子路线选项
7. 加载子路线文件 `./routes/{routeId}/{subRouteId}.md`，获取视觉指纹、过渡模板库、默认主体、默认画幅、场景道具、负面词补充
8. 锁定图片风格：用户指定 → 使用用户指定；未指定 → 默认单反写真质感
9. 组装输出契约（按 `output-contract.md` 规定的格式）
10. 应用质量门（通用质量门 + 破壁专项质量门）
11. 返回结果
```

- [ ] **Step 2: 更新分步交互规则**

将第 63-69 行：
```
## 分步交互规则

以下两种未完成状态，仅返回对应选择项，不生成最终提示词：

- **路线未选**（大路线缺失）：返回 8 大路线的中文名 + 一句话简介，供用户选择
- **子路线未选**（大路线已选、子路线缺失）：返回该大路线下所有子路线的中文名 + 核心触发关键词，供用户选择

图片风格不再阻塞流程：未指定时默认使用单反写真质感。
```
改为：
```
## 分步交互规则

快速模式优先：用户输入满足快速模式条件时，一步直接输出结果，无任何交互步骤。

以下两种未完成状态，仅返回对应选择项，不生成最终提示词：

- **路线未选**（大路线缺失且快速模式不适用）：返回 8 大路线的中文名 + 一句话简介，供用户选择
- **子路线未选**（大路线已选、子路线缺失且用户输入无法唯一匹配）：返回该大路线下所有子路线的中文名 + 核心触发关键词，供用户选择。若用户输入关键词可唯一命中（如"手机"唯一命中 phone-screen），则直接锁定，不返回选择列表

图片风格不再阻塞流程：未指定时默认使用单反写真质感。
```

- [ ] **Step 3: 验证**

用 subagent 发送 "破壁，小红书，全身"，检查是否一步输出结果（无交互）。再发送 "破壁，老照片，有创意"，检查是否走正常流程（问子路线/图片风格）。

- [ ] **Step 4: Commit**

```bash
git add .opencode/skills/image2-reality-breakthrough/SKILL.md
git commit -m "feat: 一键生成快速模式，用户输入简洁时一步出结果"
```

---

### Task 4: #9 反向破壁 — SKILL.md 部分

**Files:**
- Modify: `SKILL.md:122-131` (破壁角度与构图之后，插入破壁方向章节)
- Modify: `SKILL.md:240-251` (可选变化参数)

**Interfaces:**
- Consumes: 现有破壁框架
- Produces: 破壁方向维度（正向 `outward` / 反向 `inward`），反向破壁视觉指南，触发关键词

- [ ] **Step 1: 在破壁角度与构图之后插入破壁方向章节**

在第 131 行后插入：
```markdown

## 破壁方向（正向/反向）

除了默认的「人物从媒介中冲出」的正向破壁，支持反向破壁——人物从现实世界被吸入媒介。

| 方向 | ID | 视觉描述 | 叙事感 | 触发词 |
|------|-----|---------|--------|--------|
| 正向破壁（默认） | `outward` | 人物从媒介中冲出/走出，2D→3D | 自由、突破、力量 | 无（默认） |
| 反向破壁 | `inward` | 现实人物正在被吸入媒介，3D→2D | 迷失、穿越、悬念 | 反向破壁、被吸进去、被拉进去、从现实进入、反向 |

### 反向破壁视觉指南

反向破壁的核心是"媒介在吞噬人"——上半身仍是 3D 真人，但下半身正在像素化/油墨化/平面化，被一股力量拖入媒介。

**反向破壁的四段式模板结构（与正向对称）：**
- 完全 3D 区域：人物身体仍处于现实空间的部分，完全立体写实，真实皮肤纹理，有逃离的动态感（挣扎、后仰、伸手抓住现实物体）
- 过渡融合区域：身体被媒介吸入的分界处，指尖/发梢/衣角已经开始变成 2D 平面影像，媒介边缘有向内吸拢的视觉引力感（漩涡、光线汇聚、碎片倒流进入媒介）
- 完全 2D 区域：已被吸入媒介的身体部分，完全呈现该媒介的平面质感，已无 3D 体积感
- 过渡方式：现实人物正在被吸入媒介平面，身体 3D→2D 反向过渡，媒介边缘有吞噬性的视觉引力

**反向破壁关键视觉特征：**
- 人物表情：挣扎/惊讶/迷离/不舍，不是正向破壁的自由和喜悦
- 头发和衣物飘动方向：向媒介方向飘动（被吸进去），而非向外飞扬
- 媒介边缘：漩涡、光线汇聚、碎片倒流，强调"吸入"的视觉引力
- 手部：可能抓住现实中的物体（桌子边缘、画框），试图抵抗吸入
- 媒介残留：进入媒介的身体部分边缘有正在平面化的过渡痕迹

**选用规则：** 用户说"反向破壁""被吸进去""被拉进去""从现实进入XX""反向"时，切换到 `inward` 方向。子路线文件中标注了反向模板的优先使用。
```

- [ ] **Step 2: 在可选变化参数中增加破壁方向**

将第 240-251 行的可选变化参数改为：
```markdown
提供 9 个维度的变化建议：

1. **破壁方向**：正向破壁（从媒介中出来）/ 反向破壁（被吸入媒介）
2. **过渡效果类型**：水平切面式 / 斜向切面式 / 撕裂破洞式 / 溶解像素式 / 碎裂飞溅式 / 波动涟漪式
3. **破壁姿势**：俯身探出头 / 迈步走出式 / 侧身滑出式 / 跪爬浮现式 / 仰身冲出式 / 伸手向前式
4. **过渡程度**：轻微探出（头肩）/ 半身破壁（胸腹以上）/ 全身走出（脚踩现实）
5. **破壁角度**：正面穿出 / 斜侧45度 / 侧方滑出 / 俯角冲出 / 仰角浮现
6. **画幅**：竖版 3:4 / 竖版 9:16 / 横版 16:9 / 正方形 1:1
7. **光线**：自然光 / 侧光硬影 / 逆光轮廓 / 冷暖对比
8. **服装**：休闲日常 / 正装 / 运动风 / 国风
9. **媒介风格**：同一路线下可微调媒介的年代感、色彩倾向、磨损程度
```

- [ ] **Step 3: 验证**

用 subagent 发送 "反向破壁，从小红书里被吸进去"，检查是否生成反向破壁提示词（人物被吸入手机，而非从手机中出来）。

- [ ] **Step 4: Commit**

```bash
git add .opencode/skills/image2-reality-breakthrough/SKILL.md
git commit -m "feat: 新增反向破壁方向（3D→2D吸入式），含视觉指南和触发规则"
```

---

### Task 5: #9 反向破壁 — 子路线模板追加

**Files:**
- Modify: `routes/screen/phone-screen.md` (追加反向模板)
- Modify: `routes/social-app/xiaohongshu.md` (追加反向模板)
- Modify: `routes/art-painting/oil-painting.md` (追加反向模板 + 升级为模板库格式)

**Interfaces:**
- Consumes: SKILL.md 中定义的反向破壁视觉指南
- Produces: 3 个子路线各新增 1 个反向破壁模板

- [ ] **Step 1: phone-screen.md 追加反向模板**

在 phone-screen.md 的过渡模板库末尾（模板E之后）追加：

```markdown
### 模板F：反向破壁 — 像素吸入式
- 完全 3D 区域：头部、肩颈、上半身及一只伸向现实的手臂，完全立体写实，真实皮肤纹理，表情挣扎惊讶，手臂试图抓住桌面边缘抵抗吸入，头发和衣物向手机屏幕方向飘动，有明确的体积感和光影层次
- 过渡融合区域：胸腰交界处被手机屏幕吸入，屏幕边缘有向内汇聚的漩涡光效、像素粒子逆流回屏幕，手指尖已经开始像素化变成 2D 影像，衣角被拖入屏幕平面
- 完全 2D 区域：屏幕内的腰以下及腿部，已被完全吸入手机，呈现手机照片的 2D 平面影像，有像素网格、屏幕反光，呈现明确的平面照片质感，无体积感
- 过渡方式：现实人物正在被吸入手机屏幕，身体 3D→2D 反向过渡，屏幕边缘有吞噬性的像素漩涡，人物挣扎中部分身体已变成 2D 照片
```

- [ ] **Step 2: xiaohongshu.md 追加反向模板**

在 xiaohongshu.md 的过渡模板库末尾（模板D之后）追加：

```markdown
### 模板E：反向破壁 — 滤镜色彩吸入式
- 完全 3D 区域：头部、肩颈、上半身及一只伸向现实的手臂，完全立体写实，真实皮肤纹理和精致妆容，表情惊讶不舍，手指试图抓住桌面上的咖啡杯，头发和衣物向手机屏幕方向飘动
- 过渡融合区域：胸腰交界处被手机屏幕吸入，屏幕边缘有向内汇聚的滤镜色彩漩涡、心形和星形图标倒流回屏幕，发梢开始被像素化和滤镜染色，指尖已经开始变成平面笔记封面质感
- 完全 2D 区域：屏幕内的腰以下及腿部，已被完全吸入手机，呈现笔记封面图中的 2D 平面影像，有屏幕像素网格和滤镜色彩，呈现明确的平面图片质感，无体积感
- 过渡方式：现实博主正在被吸入种草笔记 App 屏幕，身体 3D→2D 反向过渡，屏幕边缘有滤镜色彩漩涡，人物挣扎中部分身体已变成 2D 笔记封面影像
```

- [ ] **Step 3: oil-painting.md 升级为模板库格式 + 追加反向模板**

oil-painting.md 当前是旧格式（单一过渡模板），需要先升级为模板库格式，再追加反向模板。将整个文件替换为：

```markdown
# 油画

## 触发关键词
油画、oil painting、从油画里出来、油画人物、画布人物

## 视觉指纹
一幅装在华丽画框里的油画挂在墙上，画布中的人物身体直接穿过画布平面，以真实 3D 立体质感从油画布里走出来，另一部分仍留在画布内以 2D 油画笔触形式存在，颜料的肌理感和厚重感在过渡区域清晰可见，真实的展厅光线照在人物和画框上。

## 过渡模板库

### 模板A：颜料肌理渐变 + 俯身探出头（默认推荐）
- 完全 3D 区域：头部、肩颈、上半身，完全立体写实，真实皮肤纹理，有明确的体积感和光影层次，人物从画布中探出，明显在画框平面之前
- 过渡融合区域：腰胯部与画布平面重合处，有油画颜料肌理渐变、厚涂笔触融入、色彩层叠的效果，仿佛颜料正在变成真实肌肤
- 完全 2D 区域：画面下半部分及腿部，是纯油画效果，有明显的画笔笔触、颜料肌理、调色刀痕迹、画框边缘，呈现明确的平面绘画质感，无体积感
- 过渡方式：人物身体直接穿过油画布面平面，画布之前为 3D 真人，画布之内为同一人的 2D 油画人像，以画布平面为分界面形成 2D→3D 破壁错觉

### 模板B：画布撕裂 + 迈步走出
- 完全 3D 区域：头部、躯干、双臂及一条腿的大部分，已完全走出画布，前脚踩在真实的木地板上，重心在前脚，身体微微前倾呈迈步动态，真实皮肤纹理、服装面料褶皱清晰分明
- 过渡融合区域：另一条腿的小腿处与画布平面重合，画布被撕开一道不规则裂口，边缘有纤维毛边和翻卷，厚涂油画颜料在撕裂处被拉伸变形，人物从裂口中走出
- 完全 2D 区域：画布内的后脚及鞋履，是纯油画效果，有明显的画笔笔触和颜料肌理，呈现明确的平面绘画质感，无体积感
- 过渡方式：人物从油画布的撕裂缺口中迈步走出，画布之前为 3D 真人，画布之内为同一人的 2D 油画，以画布平面为分界面形成 2D→3D 破壁错觉

### 模板C：笔触溶解 + 侧身滑出
- 完全 3D 区域：头部、一侧肩、同侧手臂及上半身，以侧面角度从画布中滑出，身体呈优雅的对角线构图，真实皮肤纹理，古典优雅的气质，有明确的体积感和光影层次
- 过渡融合区域：从肩部斜向延伸至髋部的斜线过渡带，油画笔触在过渡处逐渐溶解为颜料颗粒又凝聚为真人皮肤，调色刀痕迹沿身体轮廓延伸，颜料颗粒在空中飘散
- 完全 2D 区域：画布内的另一侧身体及腿部，是纯油画效果，有明显的画笔笔触和颜料肌理，呈现明确的平面绘画质感，无体积感
- 过渡方式：人物侧身从油画布中滑出，身体在笔触溶解与凝聚中完成 2D→3D 的过渡，以画布平面为分界面形成破壁错觉

### 模板D：反向破壁 — 颜料吸入式
- 完全 3D 区域：头部、肩颈、上半身及一只伸向画框外的手臂，完全立体写实，真实皮肤纹理，表情沉静迷离，手指试图抓住画框边缘，头发和衣物向画布方向飘动
- 过渡融合区域：腰胯部正在被画布吸入，画面边缘有向内汇聚的颜料漩涡、笔触逆流回画布，指尖开始变成油画颜料的厚涂质感，衣角被拖入画布平面变成笔触纹理
- 完全 2D 区域：画布内的下半身及腿部，已被完全吸入画布，呈现纯油画效果，有明显的画笔笔触、颜料肌理、调色刀痕迹，呈现明确的平面绘画质感，无体积感
- 过渡方式：现实人物正在被吸入油画布中，身体 3D→2D 反向过渡，画布边缘有颜料漩涡，人物挣扎中部分身体已变成油画笔触

## 默认主体气质
原创成年东方女性，20岁及以上，气质古典优雅，五官立体深邃，眼神沉静神秘，像一幅古典名画中的人物。

## 默认画幅
3:4 竖版构图，超高清，高分辨率，细节清晰，生成1张独立图片，不要合并，不要拼图

## 场景与道具
美术馆或私人展厅墙面、华丽金框油画（虚构画作）
油画元素：厚重油彩肌理、画笔笔触、调色刀痕迹、古典画风、画框雕花
环境道具：木地板、展厅射灯、旁边另一幅画的边缘、长椅一角
光线：展厅顶光射灯，画框有高光，人物有真实光影
人物身体穿过画布平面，微微向前探出（正向）或被吸入（反向）
光影：破壁人物在木地板和画框上投下真实的阴影，增强真实感

## 负面词补充
真实名画抄袭、真实画家风格像素级复制、两个人物、两张脸、画框效果、手扶画框边缘、漂浮感、完全脱离画布、笔触过渡不自然、画内人物与真人不一致、解剖错误、手指畸形、多余的手、塑料皮肤、CGI感、低清晰度、颜料肌理不真实、2D和3D无质感差异、腰斩感、水平硬切腰部、僵硬直立站姿、无动态感
```

- [ ] **Step 4: 验证**

用 subagent 发送 "反向破壁，从油画里被吸进去"，检查是否生成反向破壁提示词。

- [ ] **Step 5: Commit**

```bash
git add .opencode/skills/image2-reality-breakthrough/routes/screen/phone-screen.md
git add .opencode/skills/image2-reality-breakthrough/routes/social-app/xiaohongshu.md
git add .opencode/skills/image2-reality-breakthrough/routes/art-painting/oil-painting.md
git commit -m "feat: 3个热门子路线新增反向破壁模板，oil-painting升级为模板库格式"
```

---

### Task 6: #10 AI 生成界面子路线

**Files:**
- Create: `routes/digital-ui/ai-generation.md`
- Modify: `routes/digital-ui/_registry.md` (子路线表 + 匹配规则)
- Modify: `SKILL.md:47` (digital-ui 子路线数 5→6)

**Interfaces:**
- Consumes: SKILL.md 模板库/姿势库/角度库规范
- Produces: 新子路线（ai-generation），含 4 套过渡模板 + 完整元数据

- [ ] **Step 1: 创建 ai-generation.md**

```markdown
# AI 生成界面

## 触发关键词
AI生成、AI绘画、正在生成、生成中、从AI里出来、AI界面、生成界面

## 视觉指纹
一个 AI 图像生成工具的界面，画面中央是一张正在生成的人物图像，从模糊噪声到清晰写真，人物从生成进度中破壁而出，界面有进度条、提示词输入框、风格标签等元素，画面充满"我正在生成我自己"的元叙事趣味。

## 过渡模板库

### 模板A：像素溶解 + 俯身探出头（默认推荐）
- 完全 3D 区域：头部、肩颈、上半身及一只手臂，从生成预览区中探出，完全立体写实，真实皮肤纹理，眼神带着从数字世界诞生的好奇，有明确的体积感和光影层次
- 过渡融合区域：胸腰交界处与生成预览区平面重合，身体在过渡处从像素噪声逐渐凝聚为真人，界面进度条显示 98%，风格标签（写实/摄影/电影感）和种子数值位闪烁，像素颗粒沿身体轮廓向上飘散
- 完全 2D 区域：预览区内的腰以下及腿部，仍处于 AI 生成过程中的半模糊状态，有像素马赛克和噪声纹理，呈现明确的 AI 生成图像质感，无体积感
- 过渡方式：人物从 AI 生成预览区中探出上半身，身体在像素噪声中凝聚为 3D 真人，预览区内为同一人的 2D 生成中影像，以生成界面为分界面形成 2D→3D 破壁错觉

### 模板B：波动涟漪 + 全身走出
- 完全 3D 区域：头部、躯干、双臂及一条腿的大部分，已完全走出 AI 界面，前脚踩在真实桌面上，重心在前脚，身体微微前倾呈迈步动态，真实皮肤纹理、服装面料褶皱清晰分明
- 过渡融合区域：另一条腿的小腿处与界面平面重合，AI 界面像水面一样产生波纹涟漪，提示词输入框中的抽象色块在涟漪中扭曲变形，身后飘散着像素噪声和生成进度粒子
- 完全 2D 区域：界面内的后脚及鞋履，仍处于 AI 生成过程中的半模糊状态，有像素马赛克和噪声纹理，呈现明确的 AI 生成图像质感，无体积感
- 过渡方式：人物从 AI 生成界面中迈步走出，一只脚已踏入现实，界面呈涟漪波动，身后带着像素噪声残留，以生成界面为分界面形成 2D→3D 破壁错觉

### 模板C：碎裂飞溅 + 仰身冲出
- 完全 3D 区域：头部、肩颈、上半身及双臂，完全立体写实，真实皮肤纹理，头发和衣物向后飞扬有动感，双臂微张仿佛刚冲出生成界面，有明确的体积感和光影层次
- 过渡融合区域：胸肩区域周围的 AI 界面玻璃呈放射状碎裂，多块碎片飞溅悬浮在空中，碎片中倒映着不同阶段的生成预览（噪声→模糊→清晰），风格标签和种子数值从碎裂处飞出
- 完全 2D 区域：界面内的腰以下及腿部，仍处于 AI 生成过程中的半模糊状态，透过未碎裂的界面区域可见，有像素马赛克和噪声纹理，呈现明确的 AI 生成图像质感，无体积感
- 过渡方式：人物冲破 AI 生成界面，界面玻璃呈放射状碎裂，碎片中倒映着生成过程，人物从碎裂缺口中冲出，界面外为 3D 真人，界面内为同一人的 2D 生成中影像

### 模板D：伸手向前 + 生成中
- 完全 3D 区域：一只手和前臂完全伸出 AI 界面，手指已完全真实立体，手部有真实皮肤纹理和体积感，手指仿佛在触碰生成按钮，是画面的视觉焦点
- 过渡融合区域：手腕和前臂中部与生成界面平面重合，手背还带着像素马赛克和轻微噪声，界面显示"生成中 78%"，进度条光效在手腕处过渡
- 完全 2D 区域：界面内的身体其余部分（头部、躯干、另一条手臂、双腿），仍处于 AI 生成过程中的半模糊到清晰阶段，有像素马赛克和噪声纹理，呈现明确的 AI 生成图像质感，无体积感
- 过渡方式：人物的一只手臂穿过 AI 生成界面伸向观众，手和前臂已为 3D 真人质感，身体其余部分仍在界面内为 2D 生成中影像，以生成界面为分界面形成 2D→3D 破壁错觉

## 默认主体气质
原创成年东方女性，20岁及以上，气质知性现代，五官精致，眼神带着从数字世界诞生的好奇和新鲜感，仿佛刚刚被 AI 生成完毕、第一次看见真实世界。

## 默认画幅
3:4 竖版构图，超高清，高分辨率，细节清晰，生成1张独立图片，不要合并，不要拼图

## 场景与道具
电脑显示器或平板屏幕（无品牌标识），显示着虚构 AI 图像生成工具的界面
界面元素：生成进度条（50%-100% 之间的数值）、提示词输入框（抽象渐变色块替代文字）、分辨率选择标签（4K/8K）、风格标签（写实/摄影/电影感，用抽象色块替代）、种子数值位（数字占位符）、生成按钮（发光高亮）、预览窗口（中央大图区）
人物身体穿过生成界面平面，采用俯身或迈步姿势
背景：干净简约的现代桌面，轻微景深虚化
光线：屏幕冷光 + 柔和的室内环境光，人物面部有屏幕光补光
光影：破壁人物在桌面投下真实阴影，增强真实感

## 负面词补充
真实 AI 工具品牌名、真实 Midjourney/DALL-E/Stable Diffusion 界面、真实软件 UI 像素级复制、两个人物、两张脸、画框效果、手扶屏幕边框、漂浮感、完全脱离屏幕、界面文字乱码、解剖错误、手指畸形、多余的手、塑料皮肤、过度磨皮、低清晰度、屏幕内人物与真人不一致、2D和3D无质感差异、腰斩感、水平硬切腰部、僵硬直立站姿、无动态感、像素噪声过于密集模糊
```

- [ ] **Step 2: 修改 digital-ui/_registry.md**

在子路线表格末尾追加一行，并将描述从"五类"改为"六类"：

```markdown
# 数字界面成真（digital-ui）

人物从数字软件界面类媒介中破壁而出的超现实人像路线，包括网页弹窗、PS 图层面板、PPT 幻灯片、游戏加载界面、代码编辑器、AI 生成界面六类数字界面。

## 子路线表格

| 子路线 ID | 中文名 | 核心触发关键词 |
| --- | --- | --- |
| `webpage-popup` | 网页弹窗 | 网页、弹窗、webpage、popup、浏览器 |
| `photoshop-layers` | PS图层面板 | PS图层、photoshop、图层面板、ps界面 |
| `ppt-slide` | PPT幻灯片 | PPT、幻灯片、powerpoint、slide、演示文稿 |
| `game-loading` | 游戏加载界面 | 游戏加载、loading screen、游戏界面、加载画面 |
| `code-editor` | 代码编辑器 | 代码编辑器、code editor、ide、编辑器 |
| `ai-generation` | AI 生成界面 | AI生成、AI绘画、正在生成、生成中、从AI里出来、AI界面、生成界面 |
```

- [ ] **Step 3: 修改 SKILL.md 路线总览表**

将第 47 行：
```
| `digital-ui` | 数字界面成真 | 5 | 网页弹窗、PS 图层、PPT 幻灯片、游戏加载、代码编辑器 |
```
改为：
```
| `digital-ui` | 数字界面成真 | 6 | 网页弹窗、PS 图层、PPT 幻灯片、游戏加载、代码编辑器、AI生成界面 |
```

- [ ] **Step 4: 验证**

用 subagent 发送 "从AI生成界面里破壁出来"，检查是否命中 ai-generation 子路线并输出正确提示词。

- [ ] **Step 5: Commit**

```bash
git add .opencode/skills/image2-reality-breakthrough/routes/digital-ui/ai-generation.md
git add .opencode/skills/image2-reality-breakthrough/routes/digital-ui/_registry.md
git add .opencode/skills/image2-reality-breakthrough/SKILL.md
git commit -m "feat: 新增 AI 生成界面子路线（digital-ui/ai-generation）"
```

---

### Task 7: 全量验证 + 同步副本

**Files:**
- 所有已修改的 6 个文件（SKILL.md + 5 个子路线/registry 文件）

**Interfaces:**
- Consumes: Task 1-6 的所有改动
- Produces: 端到端验证通过 + 6 个副本目录同步

- [ ] **Step 1: 全量端到端验证**

用 3 个 subagent 并行测试以下场景：
1. "破壁，小红书，全身" → 一键快速模式，一步输出
2. "反向破壁，从油画里被吸进去" → 反向破壁，oil-painting 模板D
3. "从AI生成界面里破壁出来" → ai-generation 子路线
4. "从手机里出来" → 跳过子路线选择，直接 phone-screen
5. "破壁，老照片，有创意" → 正常流程（不走快速模式，问子路线）

- [ ] **Step 2: 同步到所有副本目录**

```powershell
$source = "D:\code\ImageMuse-Skills\.opencode\skills\image2-reality-breakthrough"
$targets = @(
  "D:\code\ImageMuse-Skills\.agents\skills\image2-reality-breakthrough",
  "D:\code\ImageMuse-Skills\.claude\skills\image2-reality-breakthrough",
  "D:\code\ImageMuse-Skills\.codex\skills\image2-reality-breakthrough",
  "D:\code\ImageMuse-Skills\.cursor\skills\image2-reality-breakthrough",
  "D:\code\ImageMuse-Skills\.cursor\image2-reality-breakthrough",
  "D:\code\ImageMuse-Skills\skills\image2-reality-breakthrough"
)
foreach ($t in $targets) {
  Copy-Item -Path "$source\*" -Destination $t -Recurse -Force
}
```

- [ ] **Step 3: Commit 同步**

```bash
git add -A
git commit -m "chore: 同步破壁技能V2升级到所有副本目录"
```